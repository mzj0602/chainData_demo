{
  "name": "data-logger-subgraph",
  "version": "1.0.0",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --studio data-logger",
    "create-local": "graph create --node http://localhost:8020/ data-logger",
    "remove-local": "graph remove --node http://localhost:8020/ data-logger",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 data-logger"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "^0.79.0",
    "@graphprotocol/graph-ts": "^0.35.1"
  }
}
